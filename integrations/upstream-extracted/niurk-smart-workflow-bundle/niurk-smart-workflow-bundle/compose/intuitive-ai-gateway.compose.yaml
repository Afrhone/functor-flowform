services:
  intuitive-ai-gateway:
    image: nginx:1.27-alpine
    container_name: intuitive-ai-gateway
    restart: unless-stopped
    ports:
      - "18088:80"
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
    extra_hosts:
      - "host.docker.internal:host-gateway"
